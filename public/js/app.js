

$(document).ready(function() {
  $(".devour-burger").on("click", function(event) {
    var id = $(this).data("id")
    var newBurger = $(this).data("newBurger")

    var newBurgerState = {
      devoured: newBurger
    }

    
    

})